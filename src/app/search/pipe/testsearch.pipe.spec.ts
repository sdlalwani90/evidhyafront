import { TestsearchPipe } from './testsearch.pipe';

describe('TestsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TestsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
